import type { Policy } from '@prisma/client'
export function evaluatePolicies(policies: Pick<Policy,'id'|'enforcement'|'rule'>[], input:{action:string;currentBudget?:number;newBudget?:number}) { let requiresApproval=false; const matchedPolicies: typeof policies=[]; for (const policy of policies) { const rule=policy.rule as {action?:string;operator?:string;value?:number}; if(rule.action!==input.action) continue; const change=input.currentBudget&&input.newBudget?((input.newBudget-input.currentBudget)/input.currentBudget)*100:undefined; if(rule.operator==='percent_change_gt'&&change!==undefined&&change<=Number(rule.value)) continue; matchedPolicies.push(policy); if(policy.enforcement==='BLOCK') return {allowed:false,requiresApproval:false,matchedPolicies}; if(policy.enforcement==='REQUIRE_APPROVAL') requiresApproval=true } return {allowed:true,requiresApproval,matchedPolicies} }
