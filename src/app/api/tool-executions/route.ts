import { NextResponse } from 'next/server'
import { executeToolAction, ToolExecutionError } from '@/lib/tool-execution'
import { projectScopeErrorResponse, requireProjectContextForBody } from '@/lib/project-scope'
export async function POST(request: Request) { try { const body = await request.json() as Record<string, unknown>; const context = await requireProjectContextForBody(body); const assignmentId = typeof body.employeeProjectAssignmentId === 'string' ? body.employeeProjectAssignmentId : ''; const projectToolId = typeof body.projectToolId === 'string' ? body.projectToolId : ''; const capabilityKey = body.capabilityKey === 'reference_read' || body.capabilityKey === 'reference_execute' ? body.capabilityKey : null; const actionKey = body.actionKey === 'read' || body.actionKey === 'execute' ? body.actionKey : null; if (!assignmentId || !projectToolId || !capabilityKey || !actionKey) return NextResponse.json({ error: 'INVALID_TOOL_ACTION' }, { status: 400 }); const execution = await executeToolAction({ projectId: context.project.id, employeeProjectAssignmentId: assignmentId, projectToolId, capabilityKey, actionKey, request: {}, summary: capabilityKey === 'reference_read' ? 'Run staging-safe reference read' : 'Run approved synthetic reference action' }); return NextResponse.json({ execution: { id: execution.id, status: execution.status, resultText: execution.resultText, approvalRequestId: execution.approvalRequestId } }, { status: 201 }) } catch (error) { if (error instanceof ToolExecutionError) return NextResponse.json({ error: error.code }, { status: 400 }); return projectScopeErrorResponse(error) } }
