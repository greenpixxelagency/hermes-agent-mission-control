import { NextResponse } from 'next/server'; import { prisma } from '@/lib/prisma'; import { EmployeeMarketError, createCustomEmployee } from '@/lib/employee-market'; import { projectScopeErrorResponse,requireProjectContextForBody,requireProjectContextForRequest } from '@/lib/project-scope'
export async function GET(r:Request){try{const c=await requireProjectContextForRequest(r);return NextResponse.json({employees:await prisma.employeeProjectAssignment.findMany({where:{projectId:c.project.id},include:{employee:true,skillAssignments:{where:{state:'ACTIVE'},select:{id:true,reconciliationStatus:true,skill:{select:{id:true,name:true,description:true,category:true,version:true}}},orderBy:{skill:{name:'asc'}}},runtimeAssignments:{include:{runtime:true,executions:{orderBy:{createdAt:'desc'},take:1}}},_count:{select:{taskAssignments:true}}},orderBy:{employee:{name:'asc'}}})})}catch(e){return projectScopeErrorResponse(e)}}
export async function POST(r:Request){try{const b=await r.json() as Record<string,unknown>;const result=await createCustomEmployee(await requireProjectContextForBody(b),b);return NextResponse.json(result,{status:201})}catch(e){if(e instanceof EmployeeMarketError)return NextResponse.json({error:e.code==='FORBIDDEN'?'Forbidden':'Name and role are required'},{status:e.code==='FORBIDDEN'?403:400});return projectScopeErrorResponse(e)}}
