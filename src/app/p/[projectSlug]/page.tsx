/* eslint-disable react-hooks/error-boundaries */
import { notFound } from 'next/navigation'
import { ProjectContextError, requireProjectContextBySlug } from '@/lib/project-context'

export default async function CommandCenter({ params }: { params: Promise<{ projectSlug: string }> }) { try { const context = await requireProjectContextBySlug((await params).projectSlug); return <div className="space-y-6"><div><p className="text-sm text-slate-400">RogerOS Command Center</p><h1 className="mt-1 text-3xl font-semibold">{context.project.name}</h1></div><div className="grid gap-4 md:grid-cols-3"><div className="rounded-xl border border-white/10 bg-white/[0.03] p-5"><p className="text-sm text-slate-400">Project access</p><p className="mt-2 font-medium">{context.project.role}</p></div><div className="rounded-xl border border-white/10 bg-white/[0.03] p-5"><p className="text-sm text-slate-400">Hermes Runtime</p><p className="mt-2 font-medium">Not connected in staging</p></div><div className="rounded-xl border border-white/10 bg-white/[0.03] p-5"><p className="text-sm text-slate-400">Organization</p><p className="mt-2 font-medium">{context.organization.name}</p></div></div></div> } catch (error) { if (error instanceof ProjectContextError) notFound(); throw error } }
