'use client'
import { useState } from 'react'
type Tool={id:string;tool:{id:string;name:string};_count:{permissions:number}}
type Catalog={id:string;name:string}
type Assignment={id:string;employee:{name:string}}
const levels=['NO_ACCESS','READ','DRAFT','EXECUTE_WITH_APPROVAL','FULL_EXECUTE']
export function ToolWorkspace({project,canManage,initialCatalog,initialTools,initialAssignments,initialConnections,initialExecutions}:{project:{id:string;name:string};canManage:boolean;initialCatalog:Catalog[];initialTools:Tool[];initialAssignments:Assignment[];initialConnections:Array<{id:string;name:string;status:string;enabled:boolean;projectTool:{tool:{name:string}}}>;initialExecutions:Array<{id:string;status:string;resultText:string|null;projectTool:{tool:{name:string}}}>}){
 const [tools]=useState<Tool[]>(initialTools),[catalog]=useState<Catalog[]>(initialCatalog),[assignments]=useState<Assignment[]>(initialAssignments),[assignment,setAssignment]=useState(''),[tool,setTool]=useState(''),[level,setLevel]=useState('NO_ACCESS'),[notice,setNotice]=useState('')
 const install=async(id:string)=>{const r=await fetch('/api/project-tools',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({projectId:project.id,toolDefinitionId:id})});setNotice(r.ok?'Project tool installed. Reload to view project state.':'Unable to install tool')}
 const connectDrive=async()=>{const r=await fetch('/api/connections/google-drive/start',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({projectId:project.id})});const b=await r.json().catch(()=>({}));if(r.ok&&typeof b.authorizationUrl==='string'){window.location.assign(b.authorizationUrl);return}setNotice(b.error==='DRIVE_OAUTH_NOT_CONFIGURED'?'Google Drive OAuth is not configured for this Preview environment yet.':'Google Drive connection could not start.')}
 const disconnectDrive=async(id:string)=>{const r=await fetch('/api/connections/google-drive/disconnect',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({projectId:project.id,connectionId:id})});setNotice(r.ok?'Google Drive disconnected.':'Google Drive disconnect denied.')}
 const referenceAction=async(action:'read'|'execute')=>{const reference=tools.find(t=>t.tool.name==='Reference Connector');if(!reference||!assignment){setNotice('Choose Chief of Staff and ensure the Reference Connector is installed.');return}const r=await fetch('/api/tool-executions',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({projectId:project.id,employeeProjectAssignmentId:assignment,projectToolId:reference.id,capabilityKey:action==='read'?'reference_read':'reference_execute',actionKey:action})});const body=await r.json().catch(()=>({}));setNotice(r.ok?`${action==='read'?'Reference read complete.':'Synthetic action is pending approval.'} ${body.execution?.status||''}`:body.error||'Governed action denied.')}
 const grant=async()=>{if(!assignment||!tool)return;const r=await fetch('/api/project-tools/permissions',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({projectId:project.id,employeeProjectAssignmentId:assignment,projectToolId:tool,level})});setNotice(r.ok?'Explicit permission saved. Reload to verify persistence.':'Permission update denied.')}
 return <div className="hq-rise"><p className="eyebrow">{project.name} / Tools & Connections</p><h1 className="mt-1 text-3xl font-semibold">Tool controls</h1><p className="mt-2 text-sm text-[var(--text-2)]">Project connections are scoped, credential references stay server-side, and every action passes governance.</p><section className="panel mt-6 p-5"><h2 className="text-lg font-semibold">Google Drive</h2>{(()=>{const c=initialConnections.find(x=>x.projectTool.tool.name==='Google Drive');return <><p className="mt-2 text-sm text-[var(--text-2)]">{c?`${c.enabled?c.status:'Disconnected'} · selected folders and files remain project-scoped.`:'Not Connected · Drive OAuth is configured separately from dashboard sign-in.'}</p>{canManage&&(!c||!c.enabled?<button onClick={()=>void connectDrive()} className="btn-primary mt-4 px-3 py-2 text-sm">Connect Google Drive</button>:<button onClick={()=>void disconnectDrive(c.id)} className="btn-ghost mt-4 px-3 py-2 text-sm">Disconnect Google Drive</button>)}</>})()}</section><section className="panel mt-6 p-5"><h2 className="text-lg font-semibold">Connections</h2>{initialConnections.length?initialConnections.map(c=><p key={c.id} className="mt-2 text-sm">{c.projectTool.tool.name} · {c.name} · {c.enabled?c.status:'Disabled'}</p>):<p className="mt-2 text-sm text-[var(--text-2)]">Not Connected</p>}</section><div className="mt-6 grid gap-3 md:grid-cols-2 xl:grid-cols-3">{catalog.map(item=>{const installed=tools.find(t=>t.tool.id===item.id);const connected=initialConnections.find(c=>c.projectTool.tool.name===item.name);return <article key={item.id} className="panel p-4"><div className="flex justify-between gap-2"><h2 className="font-semibold">{item.name}</h2><span className="text-xs text-[var(--text-3)]">{connected?.status||installed?'Not Connected':'Available'}</span></div><p className="mt-3 text-sm text-[var(--text-2)]">{installed?`${installed._count.permissions} explicit employee permission(s)`: 'Not installed for this project.'}</p>{canManage&&!installed&&<button onClick={()=>void install(item.id)} className="btn-primary mt-4 px-3 py-1 text-xs">Install metadata</button>}</article>})}</div>{canManage&&<section className="panel mt-8 p-5"><h2 className="text-lg font-semibold">Employee permissions & governed reference actions</h2><p className="mt-1 text-sm text-[var(--text-2)]">Default deny. The synthetic reference action never uses a live account.</p><div className="mt-4 grid gap-2 md:grid-cols-4"><select aria-label="Employee" value={assignment} onChange={e=>setAssignment(e.target.value)} className="rounded border border-[var(--line)] bg-transparent p-2"><option value="">Employee</option>{assignments.map(a=><option key={a.id} value={a.id}>{a.employee.name}</option>)}</select><select aria-label="Installed tool" value={tool} onChange={e=>setTool(e.target.value)} className="rounded border border-[var(--line)] bg-transparent p-2"><option value="">Installed tool</option>{tools.map(t=><option key={t.id} value={t.id}>{t.tool.name}</option>)}</select><select aria-label="Permission level" value={level} onChange={e=>setLevel(e.target.value)} className="rounded border border-[var(--line)] bg-transparent p-2">{levels.map(v=><option key={v}>{v}</option>)}</select><button onClick={()=>void grant()} className="btn-primary px-4 text-sm">Save explicit grant</button></div><div className="mt-4 flex gap-2"><button onClick={()=>void referenceAction('read')} className="btn-ghost px-3 py-2 text-sm">Run safe reference read</button><button onClick={()=>void referenceAction('execute')} className="btn-primary px-3 py-2 text-sm">Request synthetic action</button></div></section>}<section className="panel mt-8 p-5"><h2 className="text-lg font-semibold">Recent governed executions</h2>{initialExecutions.map(e=><p key={e.id} className="mt-2 text-sm">{e.projectTool.tool.name} · {e.status}{e.resultText?` · ${e.resultText}`:''}</p>)}</section>{notice&&<p className="mt-4 text-sm text-[var(--accent)]">{notice}</p>}</div>
}
