'use client'
/* eslint-disable react-hooks/set-state-in-effect, react-hooks/exhaustive-deps */
import {FormEvent,useEffect,useState}from'react'; type A={id:string;status:string;employee:{name:string;role:string;description:string|null;type:string;status:string;systemKey:string|null};_count:{taskAssignments:number}}
export function WorkforceWorkspace({project}:{project:{id:string;name:string}}){const [items,setItems]=useState<A[]>([]),[name,setName]=useState(''),[role,setRole]=useState(''),[error,setError]=useState('');const load=async()=>{const r=await fetch(`/api/workforce?projectId=${project.id}`);if(r.ok)setItems((await r.json()).employees)};useEffect(()=>{void load()},[]);const create=async(e:FormEvent)=>{e.preventDefault();const r=await fetch('/api/workforce',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({projectId:project.id,name,role})});if(!r.ok){setError('Unable to create employee');return}setName('');setRole('');await load()};return <div className="hq-rise"><p className="eyebrow">{project.name} / Workforce</p><h1 className="mt-1 text-3xl font-semibold">Workforce</h1><p className="mt-2 text-sm text-[var(--text-2)]">Project-assigned AI employees. Staging runtimes are not connected.</p><form onSubmit={create} className="mt-6 flex flex-wrap gap-2"><input value={name} onChange={e=>setName(e.target.value)} placeholder="Employee name" className="rounded border border-[var(--line)] bg-transparent p-2 text-sm"/><input value={role} onChange={e=>setRole(e.target.value)} placeholder="Role" className="rounded border border-[var(--line)] bg-transparent p-2 text-sm"/><button className="btn-primary px-4 text-sm">Add Employee</button></form>{error&&<p className="mt-3 text-sm text-[var(--down)]">{error}</p>}<div className="mt-6 grid gap-3 md:grid-cols-2 xl:grid-cols-3">{items.map(a=><article key={a.id} className="panel p-4"><div className="flex justify-between"><h2 className="font-semibold">{a.employee.name}</h2><span className="text-xs text-[var(--accent)]">{a.employee.type==='SYSTEM'?'System Employee':'Custom'}</span></div><p className="mt-1 text-sm text-[var(--text-2)]">{a.employee.role}</p><p className="mt-4 text-xs text-[var(--text-3)]">{a.employee.systemKey==='chief-of-staff'?'Runtime: Hermes · not connected in staging':a.employee.description||'Profile foundation ready.'}</p><p className="mt-4 text-xs text-[var(--text-3)]">{a._count.taskAssignments} assigned tasks · {a.status}</p></article>)}</div></div>}
