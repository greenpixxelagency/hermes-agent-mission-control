'use client'
/* eslint-disable react-hooks/set-state-in-effect, react-hooks/exhaustive-deps, @typescript-eslint/no-explicit-any */
import{FormEvent,useEffect,useState}from'react';export function GovernanceWorkspace({project}:{project:{id:string}}){const[d,setD]=useState<any>({sops:[],policies:[]}),[kind,setKind]=useState('sop'),[title,setTitle]=useState(''),[content,setContent]=useState('');const load=async()=>{const r=await fetch(`/api/governance?projectId=${project.id}`);if(r.ok)setD(await r.json())};useEffect(()=>{void load()},[]);const save=async(e:FormEvent)=>{e.preventDefault();await fetch('/api/governance',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({projectId:project.id,kind,title,content})});setTitle('');setContent('');await load()};return <section className="mt-8 border-t border-[var(--line)] pt-6"><h2 className="text-2xl font-semibold">Governance</h2><p className="mt-1 text-sm text-[var(--text-2)]">SOPs define how work is performed. Policies define what is allowed or requires control.</p><form onSubmit={save} className="mt-4 panel p-4"><select value={kind} onChange={e=>setKind(e.target.value)} className="bg-transparent"><option value="sop">SOP</option><option value="policy">Policy</option></select><input value={title} onChange={e=>setTitle(e.target.value)} placeholder="Title" className="ml-3 bg-transparent"/><textarea value={content} onChange={e=>setContent(e.target.value)} placeholder="Procedure or plain-language policy" className="mt-3 min-h-20 w-full bg-transparent"/><button className="btn-primary px-4 py-2 text-sm">Create</button></form><div className="mt-4 grid gap-4 md:grid-cols-2"><div><h3 className="font-semibold">SOP Library</h3>{d.sops.map((s:any)=><article className="panel mt-2 p-3" key={s.id}><strong>{s.title}</strong><p className="text-xs text-[var(--text-3)]">{s.status} · active version {s.activeVersion?.version||'—'}</p></article>)}</div><div><h3 className="font-semibold">Policies</h3>{d.policies.map((p:any)=><article className="panel mt-2 p-3" key={p.id}><strong>{p.title}</strong><p className="text-sm text-[var(--text-2)]">{p.description}</p><p className="text-xs text-[var(--accent)]">{p.enforcement}</p></article>)}</div></div></section>}
